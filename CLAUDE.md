# Pip's Meadow — הקשר לסשן חדש

תיעוד החלטות ולקחים מרכזיים מהעבודה עד כה. פרטי עומק (פרומפטים, קבצים, EDL) נמצאים בקבצים שהם עצמם המקור: `bible/`, `episodes/001-real-animal-sounds-farm/`, `.claude/agents/`, `.claude/skills/`. המסמך הזה הוא מפה, לא תחליף לקריאתם.

## מבנה הפרויקט
- **ערוץ:** Pip's Meadow — YouTube kids, אנגלית, גיל יעד 2-4.
- **דמות מארחת יחידה:** Pip, קוואקה (quokka). בייבל מלא ב-`bible/characters/pip.md` — כולל פרומפט בסיס קבוע (חובה להעתיק-הדבק מילולית בכל ג'נרציה, לא לנסח מחדש), צבעים בהקס, קול נעול (ראו `bible/characters/pip-voice-audition.md`).
- **סביבת בית קבועה:** מדשאת Pip (`bible/locations/pips-meadow.md`) — פרומפט קבוע, "no tall trees", בלי house/bench/path.
- **פורמט תוכן:** רוב הפרקים בפורמט B (היברידי: AI-clip → מילת מפתח → פוטג' אמיתי → אותה מילה שוב). יחס 70% B / 30% A. **אורך פרק: עד 5 דק' סה"כ** (זה תוקן — ניסוח קודם היה סותר את עצמו).
- **סוכנים ייעודיים** ב-`.claude/agents/*.md` (13), כל אחד טוען סקילים רלוונטיים מ-`.claude/skills/*/SKILL.md` (5) לפני עבודה.
- **פרק 001** (פיילוט, "Real Animal Sounds: Farm Edition") — פרה/כבשה/תרנגולת/חזיר. סטטוס מלא ב-`episodes/001-real-animal-sounds-farm/status.md`.

## החלטות מרכזיות
1. **קונספט סדרה לפני עיצוב דמות** — נבנה סדר עבודה: מחקר שוק → קונספט סדרה → נעילת דמות → נעילת סביבה → פרק ראשון. (המשתמש עצמו תפס שניגשנו לעיצוב דמות בלי קונספט מוגדר.)
2. **דמות מארחת אחת לכל הערוץ**, לא דמות שונה לכל נושא — נבחר במפורש מול האלטרנטיבה.
3. **כלי ייצור בפועל: Google Flow** (לא OpenArt כפי שתוכנן במקור במסמכי היסוד). קריינות בפועל: **ElevenLabs** (לא "OpenArt Create Voiceover" כפי שתועד ב-style-bible המקורי — פער תיעוד שצריך לתקן בהזדמנות).
4. **הרכבת הווידאו נעשית ב-ffmpeg ישירות על ידי Claude** (לא ידנית ב-CapCut/DaVinci) — הוחלט אחרי שהתברר שיש גישה ל-Bash+ffmpeg בסשן.
5. **בעיה פתוחה: lip-sync אמיתי — נמצא פתרון עובד (FAL.AI), ממתין ליישום מלא בפרק 001.** הדרישה של המשתמש: שפתי Pip יזוזו בסנכרון עם הקריינות בפועל. Google Flow/Veo מסנכרן שפתיים רק לאודיו שהוא עצמו מייצר, לא לאודיו חיצוני שמועלה. Higgsfield (`wan2_7`/`seedance_2_0` עם `audio_references`) נבדק כפתרון טכני תקין אך **חסום תקציבית** (0 קרדיטים, מנוי $49/חודש) — לא נבחר.
   - **גישה ראשונה שנכשלה: מודלי video-to-video lip-resync (דיבוב).** נבדקו `fal-ai/latentsync` (נכשל — `face_detection_error`, לא מזהה פנים קרטוניות/סטייליזד בשוט רחוק), ו-`fal-ai/sync-lipsync` v1.9 ו-v2/pro (שניהם רצו בהצלחה טכנית אך **לא ביצעו לייפ-סינק אמיתי** — אומת ע"י diff פיקסלים בין פריימי מקור/תוצר: הבדל ממוצע ~3/255, כמעט זהה למקור). **שורש הבעיה:** מודלים מהקטגוריה הזו הם כלי **דיבוב** — הם מתזמנים מחדש תנועת פה שכבר קיימת ורציפה בווידאו המקור לאודיו חדש; הם לא מייצרים תנועת דיבור מאפס. קליפ ה-AI הגולמי של Pip מ-Google Flow מכיל רק פרץ קצר של "פה פתוח" (מתוך אנימציית "היי!") ואז פה סגור/אידל — אין למודל חומר גלם של תנועת דיבור לעבוד עליו. נבדק גם קרופ הדוק על הפנים (בהשערה שגודל הפרצוף בפריים הוא הבעיה) — לא שינה את המסקנה, מה שאישר שזו בעיית קטגוריה ולא בעיית פריימינג.
   - **הפתרון שעבד: `fal-ai/kling-video/ai-avatar/v2` (standard) — image-to-video, audio-driven avatar generation.** בשונה מדיבוב, המודל בונה את **כל הביצוע מאפס** מתמונת רפרנס סטטית + קובץ אודיו (לא עורך קליפ קיים) — מתואם במפורש לתמיכה ב"cartoons and stylized characters". נבדק על מקטע OPEN: משך הפלט תאם בדיוק לאורך הקריינות (4.8 שנ', לא ל-4.0 שנ' של קליפ ה-AI המקורי), תנועת השפתיים השתנתה באופן רציף ואמיתי על פני כל הקליפ, ואושר ע"י המשתמש בהאזנה/צפייה בפועל ("יצא מעולה!").
   - **עלות מדויקת (מחירון רשמי FAL.AI):** Standard $0.0562/שנ' (~$4.78 לכל פרק 001, 7 מקטעי Pip, ~85 שנ' סה"כ), Pro $0.115/שנ' (~$9.78 לפרק) — תשלום לפי שימוש, בלי מנוי חודשי. משמעותית זול מ-Higgsfield.
   - **גישה ל-FAL.AI:** מאומתת ועובדת — הפרוקסי מזריק credentials אוטומטית ל-`*.fal.run`/`*.fal.ai`/`*.fal.media`. תהליך: להעלות תמונת רפרנס + קובץ קריינות ל-fal storage (`rest.alpha.fal.ai/storage/upload/initiate` → PUT → קבלת `file_url`), לשלוח ל-`queue.fal.run/fal-ai/kling-video/ai-avatar/v2/standard` עם `image_url`+`audio_url`, לפולל `status_url` עד `COMPLETED`, להוריד מה-`video_url` שמוחזר. אתר השיווק `fal.ai` עצמו חסום ע"י מדיניות רשת (403), אבל ה-API האמיתי (`queue.fal.run`, `rest.alpha.fal.ai`, `v3b.fal.media`) עובד תקין.
   - **הורצו COW/SHEEP/CHICKEN/PIG/RECAP (Standard).** תמונת רפרנס לכל חיה = פריים ראשון מהקליפ המאושר (Pip + החיה יחד), אודיו = חיתוך ל-11.1 שנ' הראשונות (החלק שבו Pip על המסך לפי `edit.md`; השאר מתנגן על פוטג' אמיתי). RECAP = קריינות מלאה. **נמצאה בעיה שחוזרת על עצמה: כשתמונת הרפרנס מכילה יותר מדמות אחת (Pip + החיה), המודל מנפיש גם את פי החיה כאילו היא מדברת** — לא רק את Pip. תועד כלקח ב-`ai-visual-consistency` skill. **לפעם הבאה: להשתמש בתמונת רפרנס עם Pip בלבד** (לחתוך/לייצר תמונה נטולת דמות שנייה), ולשלב את הפלט מחדש בקומפוזיציה המלאה בעריכה אם צריך לשמור על שוט משולב. בפועל הפרק הזה לא תוקן רטרואקטיבית — הוחלט להתעלם מהבעיה בפרק 001 (ראה status.md).
   - **משך פלט לא תואם בדיוק לאורך האודיו שהוזן** — Kling מעגל לבלוקים קבועים (12.27s פלט לעומת 11.1s אודיו לחיות; 18.13s לעומת 17.95s ל-RECAP). לוקחים בחשבון בעריכה.
   - **CLOSE הושלם** (קריינות התקבלה מהמשתמש, תמונת רפרנס Pip-בלבד — לא כלל את בעיית "דמות שנייה" כי אין דמות נוספת בשוט).
   - **בעיה חדשה שנמצאה ותוקנה: אלמנטים משניים בתמונת הרפרנס (לא רק הדמות הראשית) יכולים לסטות לצורה שונה לגמרי, לא רק "לדבר".** ב-RECAP, תמונת הרפרנס המקורית הכילה 4 בועות אייקון קטנות (פרה/כבשה/תרנגולת/חזיר) מסביב ל-Pip — כולן תקינות במקור. אחרי ה-avatar-generation, בועת הכבשה סטתה לפרצוף דמוי-תינוק-אנושי (עיניים עצומות, פה מחייך, בלי מרקם צמר) — בעיה חזותית חריפה יותר מ"החיה מדברת", כי זה נראה כמו פרצוף אדם לא-במקום בתוכן לילדים.
   - **הפתרון הסופי שיושם (גרסה 2, לאחר שגרסה 1 נדחתה):** ניסיון ראשון היה לחתוך תמונת רפרנס Pip-בלבד ולהרכיב בחזרה **תמונות סטטיות** של האייקונים כ-overlay — המשתמש דחה את זה כ"נראה מודבק/שטוח, לא משתלב עם התאורה". **הפתרון שהתקבל בפועל:** במקום ליצור מחדש ב-Flow, נעשה שימוש **בווידאו המאושר הקיים** (`001-recap-pip-meadow.mp4`, שבו 4 הבועות תקינות ומונפשות היטב — Flow לא היה הבעיה, ה-avatar-generation היה) — ממנו נחתכו **4 קליפי-וידאו** (לא תמונות) של אזורי הבועות (עם התנועה/הקפיצה המקורית), הורחבו ל-18 שנ' בטכניקת **ping-pong** (per הכלל התיעודי למטה — לא frozen frame), והורכבו כ-overlay על גבי ה-Pip המסונכרן-שפתיים (שהופק מרפרנס Pip-בלבד, נטול בועות). כך גם ה-lip-sync מדויק וגם הבועות מונפשות אמיתיות שמשתלבות ברקע (אותה סביבה נעולה בדיוק). תועד ב-`ai-visual-consistency` skill.
   - **לקח נוסף:** קרופ-למרכז אוטומטי אחרי חיתוך תמונת רפרנס לא בהכרח נשאר ממורכז בפלט — נדרש לבדוק כמה פריימים על פני כל האורך (לא רק פריים אחד) לפני קביעת offset סופי; ב-RECAP הפה נחתך לגמרי בקרופ הראשוני ולא היה נראה מהפריים הבודד שנבדק.
   - **בעיה נוספת שתועדה (לא תוקנה, התקבלה כפי שהיא):** כשהתסריט מתאר פעולה פיזית (למשל "Sniff, sniff...") — ה-avatar מסנכרן שפתיים למילים אבל לא מבצע את הפעולה בגוף. לשקול בתסריטים עתידיים.
   - **טרם נפתר / נותר ליישום:** בדיקת עקביות ויזואלית סופית מול ה-Bible על כל 7 המקטעים; קבצי הטסט ב-`episodes/001-real-animal-sounds-farm/assets/lipsync-tests/`; הפרק המורכב המלא (rough cut, 1:57) ב-`assets/assembled/001-full-episode-draft.mp4` — עדיין חסרים כתוביות/טקסט-על-מסך.

## לקחים טכניים (ai-visual-consistency skill מעודכן עם כולם)
- **המודל מוסיף אלמנטים שלא ביקשו** — תופעה חוזרת, לא מקרה בודד: הוסיף בית/ספסל/שביל לסצנת OPEN, פרה עם פרטי-זיהוי שגויים (טרקטור, חבילות שחת, פעמון) בניסיון עצמאי, אף חייה תרנגול במקום תרנגולת בפוטג' אמיתי (Pexels). **תמיד להשוות תוצר מול ה-Bible, לא רק מול "האם זה נראה טוב".**
- **הרצה חוזרת של אותו פרומפט טקסטואלי אינה ריפרודוקטיבית** — אותו טקסט יכול להפיק תוצאה שונה משמעותית בכל הרצה. תוצר מאושר = נכס נעול; לא להניח שהרצה חוזרת "של אותו דבר" תשחזר אותו.
- **Frames to Video ≠ Ingredients to Video** — Frames נועל את הפריים ההתחלתי (טוב לאנימציה של סטיל מאושר בלי שינוי עיצוב), Ingredients מאפשר לפרש-מחדש קומפוזיציה (טוב ליצירת שוט חדש מכמה רפרנסים). Frames יכול עדיין להמציא תוכן בקצוות הפריים (למשל עצים) — צריך לחזור על כל בלוק ה-negative-prompt של הסביבה גם בפרומפט הווידאו, לא להניח שהוא "עובר" מפרומפט התמונה.
- **מגבלת 3 ingredients ב-Flow** — נפתרה ע"י המשתמש: איחוד כמה רפרנסים (4 חיות) לתמונת קולאז' אחת, שנספרת כ-ingredient בודד.
- **הקפאת פריים אחרון ("frozen frame") כדי לכסות פער בין אורך קליפ AI לאורך קריינות — אסור, נראה תקוע ולא מקצועי.** נבדק בפועל בפרק 001 (feedback חד-משמעי מהמשתמש: "לא מקצועי"). הפתרון שנקבע ותועד כחוק ב-`.claude/agents/editor.md`: **לופ הלוך-ושוב (ping-pong: forward+reverse via ffmpeg)** לפערים גדולים; "slow-motion stretch" (`setpts`) מקובל רק לפערים קטנים (עד 1-2 שנ').
- **concat דמוקסר (`-f concat`) גורם לבאגים בזמן משך פלט** כשמשלבים מקורות עם fps/קידוד שונים תוך רי-אנקוד — להשתמש ב-concat **פילטר** (`filter_complex concat`) עם נורמליזציה מפורשת (`fps=25,setpts=PTS-STARTPTS`) לכל stream לפני האיחוד.
- **zoompan על מקור וידאו אמיתי (לא סטיל) איטי מאוד** — יצר תקיעה של 10+ דקות; להסיר zoompan מפוטג' אמיתי (יש לו כבר תנועה טבעית) ולהשתמש בו רק על תמונות סטילס.
- **גישת רשת מוגבלת בסנדבוקס** — pexels.com ו-pixabay.com חסומים ("organization policy"); הפתרון בפועל: המשתמש מוריד/שולח קבצים ישירות במקום שאני אנסה להוריד מהם.

## תבנית זרימת עבודה שהתבססה לפרק
תסריט (story-writer) → אימות עובדות (fact-checker) → קריינות (voice-director) → פרומפטים (visual-prompter) → פוטג' (footage-curator) → **ייצור בפועל ידני ב-Google Flow + ElevenLabs (משתמש)** → רשימת עריכה/EDL (editor) → הרכבה ב-ffmpeg (Claude, בפועל) → חבילת פרסום (creative-packager) → **compliance-qa חוסם לפני העלאה** → ניתוח 7/28 יום (analytics-strategist).

## סטטוס פרק 001 נכון לסוף הסשן
כל הנכסים (ויזואל AI, פוטג' אמיתי, SFX, מוזיקה, קריינות מלבד CLOSE) אושרו ונעולים. הרכבה בפועל בעיצומה: OPEN ו-COW הורכבו ואושרו מבחינת תנועה/עריכה (ping-pong fix), אך **מסומנים כטעונים תיקון lip-sync** ברגע שהבעיה תיפתר. עדיין חסר: קובץ קריינות CLOSE מהמשתמש, הרכבת SHEEP/CHICKEN/PIG/RECAP/CLOSE, חיבור סופי של כל הפרק, ואז compliance-qa.

## הרגלי עבודה שהתבססו
- Commit+push לגיט אחרי כל שינוי משמעותי, עם הודעות תיאוריות.
- סריקות "gap scan" תקופתיות על כל הפייפליין כדי לתפוס פערים לפני שממשיכים (תפסו: סביבה חסרה, פרומפט RECAP חסר, ניסוחי סביבה לא אחידים).
- המשתמש קפדן מאוד על איכות ותופס בעיות בעצמו (זיהוי תרנגול, לול חזירים תעשייתי, פריים קפוא, אי-התאמת אורך אודיו/וידאו) — לתעד כל בעיה כזו כלקח בקובץ הסקיל הרלוונטי, לא רק לתקן אד-הוק.
